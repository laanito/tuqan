{% extends "main.twig" %}

{% block contenido %}
<div class="alert alert-warning" style="margin-top: 30px; max-width: 700px;">
    <h4>{{ title|raw }}</h4>
    <p>{{ message|raw }}</p>
</div>
{% endblock %}